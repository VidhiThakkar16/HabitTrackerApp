<?xml version="1.0" encoding="utf-8"?>
<!--
    item_habit.xml
    Layout for a single habit item displayed in the RecyclerView on the dashboard.
    Each item shows habit name, frequency, category, and completion checkbox.
-->
<!--  foreground (property):ripple effect on touch -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    card_view:cardCornerRadius="16dp"
    card_view:cardElevation="8dp"
    android:foreground="?android:attr/selectableItemBackground"
    android:backgroundTint="#FFFFFF">

<LinearLayout
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:orientation="horizontal"
android:padding="16dp"
android:gravity="center_vertical">

<!-- 🔹 Left Info Section: Habit details -->
<LinearLayout
    android:layout_width="0dp"
    android:layout_weight="1"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Habit Name -->
    <TextView
        android:id="@+id/txtHabitName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Habit Name"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="#1A1A1A"/>

    <!-- Habit Frequency -->
    <TextView
        android:id="@+id/txtHabitFrequency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Frequency"
        android:textSize="14sp"
        android:textColor="#555555"
        android:layout_marginTop="2dp"/>

    <!-- Habit Category -->
    <TextView
        android:id="@+id/txtHabitCategory"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Category"
        android:textSize="14sp"
        android:textColor="#6200EE"
        android:layout_marginTop="2dp"/>
</LinearLayout>

<!-- 🔹 Right Section: Completion Checkbox -->
    <!-- slightly bigger checkbox -->
<CheckBox
    android:id="@+id/chkCompleted"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:scaleX="1.2"
    android:scaleY="1.2"/>
</LinearLayout>
    </androidx.cardview.widget.CardView>
